import { ScrollEnd } from './scroll-end';

describe('ScrollEnd', () => {
  it('should create an instance', () => {
    const directive = new ScrollEnd();
    expect(directive).toBeTruthy();
  });
});
